doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title= title 
    style.
        img{
            width: 100%;
            border-radius: 12px;
            height: 214px;
            object-fit: cover;
        }

        body{
            background: #EAEFF1;
            font-family: 'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', Geneva, Verdana, sans-serif;
        }

        #conatiner{
            max-width: 300px;
            margin: 0 auto;
            margin-top: 20vh;
        }

        .card{
            background-color: #fff;
            padding: 8px;
            border-radius: 12px;
            border: solid 1px #bac0d8;
        }

        .tag{
            padding: 4px 8px;
            border:  1px solid #e5eae0;
            border-radius: 50px;
            font-size: 12px;
            font-weight: 600;
            color: #788697;
        }

        .name{
            font-size: 24px;
            font-weight: 600;
            margin-top: 16px;
        }

        p{
            font-size: 14px;
            color: #7f8c98;
            line-height: 150%;
        }

        button{
            border: none;
            padding: 12px 24px;
            border-radius: 50px;
            font-weight: 600;
            color: #0077ff;
            background-color: #e0efff;
            margin: 0 auto;
            display: block;
            cursor: pointer;
        }

        input{
            border: none;
            padding: 12px 24px;
            border-radius: 50px;
            font-weight: 600;
            color: #0077ff;
            background-color: #e0efff;
            margin: 0 auto;
            display: block;
        }

    body
        h1 Editor de Recetas
        br
        button(onclick="location.href='/'") Home
        br
        #container
            .card
                form(method='post' action='/Editar-Receta' onsubmit='action' name='form')
                    img(src='http://homesapiens.es/wp-content/uploads/2014/02/receta.jpg', alt='receta')
                    // linkreceta
                    input(type="text" name='id_rec' value=id_rec readonly)
                    input(type='text' placeholder='Nombre de la Receta' name='nom_rec' value=nom_rec)
                    br
                    input(type='text' placeholder='Descripcion de la receta' name='desc_rec' value=desc_rec)
                    br
                    input(type='url' placeholder='Imagen de la Receta (URL)' name='url_rec' value=url_rec)
                    br
                    .name Unete a la comunidad!!
                    p Crea nuevas y divertidas recetas en esta p&aacute;gina
                    input(type='submit' value='Subir') 
                    br
                    button(type='reset') Borrar datos